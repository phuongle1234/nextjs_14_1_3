const categoriesPrefix = "category"

export const categories = {
    read: `${categoriesPrefix}`,
    create: `${categoriesPrefix}/create`,
    update: `${categoriesPrefix}`,
    delete: `${categoriesPrefix}`,
}